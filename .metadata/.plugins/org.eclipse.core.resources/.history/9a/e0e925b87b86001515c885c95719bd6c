import poly_filter
import random

temp = [ random.uniform(1,10) for _ in xrange(100) ]
poly = poly_filter.poly_filter(3, 3 )
poly.update(1)
poly.update(2)
poly.update(3)
print poly.update(4)
print poly.update(71)